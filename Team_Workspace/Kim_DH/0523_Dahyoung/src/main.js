/**
 * main.js — application entry point
 *
 * Imports:
 *   layouts/  — app-shell, left-sidebar, right-sidebar
 *   views/    — home, chat, settings, account, help
 *   widgets/  — all UI widgets
 *   core/api/ — domain API modules
 *
 * Orchestration logic lives in script.js until it is fully extracted
 * into the layout/view layers above.
 */
import './script.js';
